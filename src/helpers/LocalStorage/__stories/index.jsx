import React from 'react';
import LocalStorageStory from './LocalStorageStory';

const stories = [
    {
        name: 'LocalStorage',
        story: () => (
            <div>
                <LocalStorageStory />
            </div>
        ),
    },
];

export default stories;
