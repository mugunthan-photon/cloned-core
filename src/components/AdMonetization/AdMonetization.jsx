import DFPMonetization from './DFPMonetization';
import ADSenseSearch from './ADSenseSearch';
import AdsContainer from './AdsContainer';

/*
 * Exporting All the Components here so that the consumer
 * can consume one import by destructuring one export.
 */

const AdMonetization = {
    DFPMonetization,
    ADSenseSearch,
    AdsContainer,
};

export default AdMonetization;

